// config.ts — Universal constants. Imported by everything that needs canvas dimensions or cycle timing.

/** Internal (pixel-art) resolution */
export const W = 256;
export const H = 144;

/** Cycle duration in seconds */
export const CYCLE_DURATION = 300;
