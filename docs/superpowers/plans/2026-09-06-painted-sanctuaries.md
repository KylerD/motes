# Painted sanctuaries

The user's two visual references pin the direction: saturated, atmospheric illustration with inhabited scenery, dramatic light and restful movement. They supersede the muted miniature finish. The user has already authorised implementation and creative decisions; no proposal round is needed.

## Build

- Create two original environment paintings: a neon rooftop rain garden and a golden meadow sanctuary. Keep local assets and exact generation prompts.
- Make the default watch surface a living painting, with model-driven lights in its water, animated reflections, rain or pollen, and direct ripple interaction. Animation follows simulation time and respects pause and reduced motion.
- The user's 7 September cleanup request removes the old toy pond, pixel world, journal, automation machinery, obsolete renderers and dependencies. Explore reveals connections, energy, food and submerged shelves directly in the same painted water. Panning and zooming transform the painting and cells together. Retain interventions, saves, branches and accessible controls.
- Give each setting a quiet weather sound bed alongside the existing colony music; sound still starts only on Listen.
- Keep scene selection, transport, sound and an unobtrusive hide-controls action readable on desktop and phone. Failed art loads must leave a usable fallback.

## Verify

Run model tests and production build. Verify both scenes on desktop and mobile together, including actual art loading, weather toggles, sound, pause determinism, ripple input, scene persistence, Explore round-trip and existing experiment flows. Apply one visual correction batch, confirm, then obtain a fresh bounded review. Record the finished visual system and commit locally.
